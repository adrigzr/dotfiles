local selected=$(fc -rl 1 | awk '{$1="";print substr($0,2)}' |
  _fzf_tmux_popup --pipe --tac --no-sort --exact \
    --preview 'echo {}' \
    --preview-window 'up:3:wrap')

if [ -n "$selected" ]; then
  BUFFER="$selected"
  CURSOR=$#BUFFER
fi

zle reset-prompt

# vim: ft=zsh
