# Global Instructions

- Preferred language: English
- Comments: minimal, comment why instead of what
- Always verify changes compile/lint before considering a task complete
- Prefer editing existing files over creating new ones
- Commits: imperative mood, concise summary line, reference ticket if applicable
- Node.js: use `fnm` to manage versions; use the installed major version matching `package.json`; install it first if missing
