snippet plugin Add plugin header
abbr    plugin.vim
options head
  "
  " ${1:`expand('%:t:r')`}.vim: ${2:description}
  "
  " Author: `system('git config --get user.name')[:-2]` <`system('git config --get user.email')[:-2]`>
  " License: Same as Vim itself
  "
  if exists('g:loaded_$1')
    finish
  endif
  let g:loaded_$1 = 1

  ${0}

